# Palette will be injected by Nix from Stylix
# Keep the color references but don't define the palette here

[character]
success_symbol = "[~>](bold fg:green)"
error_symbol = "[~>](bold fg:red)"

[username]
format = "[$user]($style) on "
style_user = "bold brown"
